import React from "react"; // we need this to make JSX compile
import Header from "../Header/Header";

const Help: React.FC = () => {
    return (
        <>
            <Header />
            <h1>Help</h1>
        </>
    );
};

export default Help;